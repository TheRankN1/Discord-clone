import { UserDataBaseInterface } from '../../shared/interfaces/user-data-base.interface';

export const login = (username: string, password: string): UserDataBaseInterface => {
  return {} as UserDataBaseInterface;
};
